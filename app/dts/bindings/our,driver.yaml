description: Our LED sensor driver

compatible: "our,driver"

include: base.yaml

properties:
  led-gpios:
    type: phandle-array
    required: true
    description: GPIO pin connected to the on-board LED
